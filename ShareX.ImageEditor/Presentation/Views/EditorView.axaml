<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ShareX.ImageEditor.Presentation.ViewModels"
             xmlns:views="using:ShareX.ImageEditor.Presentation.Views"
             xmlns:models="using:ShareX.ImageEditor.Core.Annotations"
             xmlns:converters="using:ShareX.ImageEditor.Presentation.Converters"
             xmlns:theming="using:ShareX.AvaloniaUI.Theming"
             xmlns:editorTheming="using:ShareX.ImageEditor.Presentation.Theming"
             xmlns:helpers="using:ShareX.ImageEditor.Presentation.Rendering"
             xmlns:controls="using:ShareX.ImageEditor.Presentation.Controls"
             xmlns:easterEggs="using:ShareX.ImageEditor.Presentation.EasterEggs"
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
             x:Class="ShareX.ImageEditor.Presentation.Views.EditorView"
             x:DataType="vm:MainViewModel"
             Focusable="True">

    <UserControl.Styles>
        <Style Selector="Border.gradient-preview-half">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style Selector="Border.gradient-preview-half.left">
            <Setter Property="CornerRadius" Value="5,0,0,5"/>
        </Style>
        <Style Selector="Border.gradient-preview-half.right">
            <Setter Property="CornerRadius" Value="0,5,5,0"/>
        </Style>
        <Style Selector="Border.gradient-preview-half:pointerover">
            <Setter Property="Background" Value="#66FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCFFFFFF"/>
        </Style>
        <Style Selector="Border.gradient-preview-half:pressed">
            <Setter Property="Background" Value="#55000000"/>
            <Setter Property="BorderBrush" Value="#DD000000"/>
        </Style>
        <Style Selector="Border.color-preview-overlay">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style Selector="Border.color-preview-overlay:pointerover">
            <Setter Property="Background" Value="#66FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCFFFFFF"/>
        </Style>
        <Style Selector="Border.color-preview-overlay:pressed">
            <Setter Property="Background" Value="#55000000"/>
            <Setter Property="BorderBrush" Value="#DD000000"/>
        </Style>
    </UserControl.Styles>

    <UserControl.DataTemplates>
        <DataTemplate DataType="{x:Type vm:ConfirmationDialogViewModel}" x:DataType="vm:ConfirmationDialogViewModel">
            <views:ConfirmationDialogView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:NewImageDialogViewModel}" x:DataType="vm:NewImageDialogViewModel">
            <views:NewImageDialogView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InsertImageDialogViewModel}" x:DataType="vm:InsertImageDialogViewModel">
            <views:InsertImageDialogView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EmojiPickerDialogViewModel}" x:DataType="vm:EmojiPickerDialogViewModel">
            <views:EmojiPickerDialogView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StartScreenDialogViewModel}" x:DataType="vm:StartScreenDialogViewModel">
            <views:StartScreenDialogView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ToolbarCustomizationDialogViewModel}" x:DataType="vm:ToolbarCustomizationDialogViewModel">
            <views:ToolbarCustomizationDialogView />
        </DataTemplate>
    </UserControl.DataTemplates>

    <UserControl.Resources>
        <converters:DoubleToCornerRadiusConverter x:Key="DoubleToCornerRadius" />
        <converters:EqualsConverter x:Key="EqualsConverter" />

        <ContextMenu x:Key="EditorContextMenu"
                     Placement="Pointer"
                     BorderBrush="{DynamicResource ShareX.Brush.Border}"
                     BorderThickness="1"
                     CornerRadius="6"
                     Padding="6,2"
                     Cursor="Arrow">
            <MenuItem Header="Undo" Command="{Binding UndoCommand}" InputGesture="Ctrl+Z">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionUndo}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Redo" Command="{Binding RedoCommand}" InputGesture="Ctrl+Y">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionRedo}"/>
                </MenuItem.Icon>
            </MenuItem>

            <Separator/>

            <MenuItem Header="Delete" Command="{Binding DeleteSelectedCommand}" InputGesture="Delete">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionDelete}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Delete all" Command="{Binding ClearAnnotationsCommand}" InputGesture="Shift+Delete">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionClearAll}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Flatten" Command="{Binding FlattenImageCommand}" InputGesture="Ctrl+Shift+F">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.LayerFlatten}"/>
                </MenuItem.Icon>
            </MenuItem>

            <Separator/>

            <MenuItem Header="Cut" Command="{Binding CutAnnotationCommand}" InputGesture="Ctrl+X">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ToolCutOut}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Copy" Command="{Binding CopyAnnotationCommand}" InputGesture="Ctrl+Shift+C">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionCopy}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Paste" Command="{Binding PasteCommand}" InputGesture="Ctrl+V">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionPaste}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Duplicate" Command="{Binding DuplicateSelectedCommand}" InputGesture="Ctrl+D">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionDuplicate}"/>
                </MenuItem.Icon>
            </MenuItem>

            <Separator/>

            <MenuItem Header="Bring to front" Command="{Binding BringToFrontCommand}" InputGesture="Home">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionBringToFront}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Bring forward" Command="{Binding BringForwardCommand}" InputGesture="PageUp">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionBringForward}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Send backward" Command="{Binding SendBackwardCommand}" InputGesture="PageDown">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionSendBackward}"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Send to back" Command="{Binding SendToBackCommand}" InputGesture="End">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionSendToBack}"/>
                </MenuItem.Icon>
            </MenuItem>

            <Separator/>

            <MenuItem Header="{Binding ToggleToolbarsMenuHeader}" Command="{Binding ToggleToolbarsCommand}" InputGesture="Ctrl+H">
                <MenuItem.Icon>
                    <TextBlock FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal" Text="{x:Static editorTheming:EditorIcons.ActionToggleToolbars}"/>
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </UserControl.Resources>

    <ThemeVariantScope x:Name="EditorThemeScope"
                       RequestedThemeVariant="{x:Static theming:ThemeManager.ShareXDark}">
        <Grid RowDefinitions="*">

        <!-- Row 0: Main Content Area with Preview and Sidebar (Background Layer) -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Background="{DynamicResource ShareX.Brush.Background.Main}">

            <!-- Left Sidebar (Settings Panel) -->
            <Border Grid.Column="0"
                    Classes="editor-sidebar left"
                    Classes.open="{Binding IsSettingsPanelOpen}"
                    Padding="0"
                    Background="{DynamicResource ShareX.Brush.Background.Panel}"
                    BorderBrush="{DynamicResource ShareX.Brush.Border}"
                    IsHitTestVisible="{Binding IsSettingsPanelOpen}">
                <ScrollViewer x:Name="SidebarScrollViewer" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                              ScrollChanged="OnSidebarScrollChanged">
                    <StackPanel Width="250" Spacing="0" Margin="16">
                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
                            <TextBlock Text="{x:Static editorTheming:EditorIcons.PanelBackground}"
                                       FontFamily="{DynamicResource ShareX.FontFamily.Icon}"
                                       VerticalAlignment="Center"
                                       Margin="0,2,0,0"/>
                            <TextBlock Text="Background"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Background Margin -->
                        <StackPanel Spacing="0">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Grid.Column="0" Text="Margin"/>
                                <Border Grid.Column="1" Classes="capsule">
                                    <TextBlock Text="{Binding BackgroundMargin, StringFormat='{}{0:F0} px'}"/>
                                </Border>
                            </Grid>
                            <Slider x:Name="BackgroundMarginSlider"
                                    Minimum="0"
                                    Maximum="200"
                                    Value="{Binding BackgroundMargin}"
                                    Background="{DynamicResource ShareX.Brush.Control.Background.Disabled}"/>
                        </StackPanel>

                        <!-- Background Padding -->
                        <StackPanel Spacing="0">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Grid.Column="0" Text="Padding"/>
                                <Border Grid.Column="1" Classes="capsule">
                                    <TextBlock Text="{Binding BackgroundPadding, StringFormat='{}{0:F0} px'}"/>
                                </Border>
                            </Grid>
                            <Slider x:Name="BackgroundPaddingSlider"
                                    Minimum="0"
                                    Maximum="200"
                                    Value="{Binding BackgroundPadding}"
                                    Background="{DynamicResource ShareX.Brush.Control.Background.Disabled}"/>
                            <controls:LabeledToggleSwitch Label="Smart padding"
                                                          IsChecked="{Binding BackgroundSmartPadding}"
                                                          IsEnabled="{Binding CanUseBackgroundSmartPadding}"
                                                          ToggleForeground="{DynamicResource ShareX.Brush.Text.Secondary}"
                                                          Margin="0,0,0,16"/>
                        </StackPanel>

                        <!-- Background Rounded Corner -->
                        <StackPanel Spacing="0">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Grid.Column="0" Text="Rounded Corner"/>
                                <Border Grid.Column="1" Classes="capsule">
                                    <TextBlock Text="{Binding BackgroundRoundedCorner, StringFormat='{}{0:F0} px'}"/>
                                </Border>
                            </Grid>
                            <Slider x:Name="BackgroundRoundedCornerSlider"
                                    Minimum="0"
                                    Maximum="50"
                                    Value="{Binding BackgroundRoundedCorner}"
                                    Background="{DynamicResource ShareX.Brush.Control.Background.Disabled}"/>
                        </StackPanel>

                        <!-- Background Shadow Radius -->
                        <StackPanel Spacing="0">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Grid.Column="0" Text="Shadow Radius"/>
                                <Border Grid.Column="1" Classes="capsule">
                                    <TextBlock Text="{Binding BackgroundShadowRadius, StringFormat='{}{0:F0} px'}"/>
                                </Border>
                            </Grid>
                            <Slider x:Name="BackgroundShadowRadiusSlider"
                                    Minimum="0"
                                    Maximum="100"
                                    Value="{Binding BackgroundShadowRadius}"
                                    Background="{DynamicResource ShareX.Brush.Control.Background.Disabled}"/>
                        </StackPanel>

                        <!-- Ratio -->
                        <StackPanel Spacing="0">
                            <TextBlock Text="Ratio" Margin="0,0,0,16"/>
                            <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,*,*">
                                <!-- Row 0 -->
                                <Button Grid.Row="0" Grid.Column="0" Content="Auto"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="Auto"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=Auto}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                <Button Grid.Row="0" Grid.Column="1" Content="1:1"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="1:1"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=1:1}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                <Button Grid.Row="0" Grid.Column="2" Content="4:3"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="4:3"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=4:3}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

                                <!-- Row 1 -->
                                <Button Grid.Row="1" Grid.Column="0" Content="3:2"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="3:2"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=3:2}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                <Button Grid.Row="1" Grid.Column="1" Content="16:9"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="16:9"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=16:9}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                <Button Grid.Row="1" Grid.Column="2" Content="5:3"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="5:3"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=5:3}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

                                <!-- Row 2 -->
                                <Button Grid.Row="2" Grid.Column="0" Content="9:16"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="9:16"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=9:16}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                <Button Grid.Row="2" Grid.Column="1" Content="3:4"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="3:4"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=3:4}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                <Button Grid.Row="2" Grid.Column="2" Content="2:3"
                                        Classes="ratio-button"
                                        Command="{Binding SetOutputRatioCommand}" CommandParameter="2:3"
                                        Classes.active="{Binding SelectedOutputRatio, Converter={StaticResource EqualsConverter}, ConverterParameter=2:3}"
                                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Grid>
                        </StackPanel>

                        <!-- Background -->
                        <StackPanel Spacing="0">
                            <TextBlock Text="Background" Margin="0,16"/>
                            <ComboBox x:Name="BackgroundTypeComboBox"
                                      ItemsSource="{Binding BackgroundModeOptions}"
                                      SelectedItem="{Binding SelectedBackgroundModeOption, Mode=TwoWay}"
                                      Margin="0,0,0,12"
                                      Padding="8,6"
                                      Background="{DynamicResource ShareX.Brush.Control.Background}"
                                      BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                      BorderThickness="1"
                                      Foreground="{DynamicResource ShareX.Brush.Text}"
                                      HorizontalAlignment="Stretch">
                            </ComboBox>

                            <StackPanel IsVisible="{Binding IsGradientBackgroundModeSelected}" Spacing="8">
                                <Grid Margin="0,0,0,4">
                                    <Border x:Name="BackgroundGradientPreviewBorder"
                                            Height="40"
                                            CornerRadius="5"
                                            BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                            BorderThickness="1"
                                            Background="{Binding BackgroundGradientBrush}"
                                            ClipToBounds="True">
                                        <Grid ColumnDefinitions="*,*">
                                            <Border Grid.Column="0"
                                                    Classes="gradient-preview-half left"
                                                    Cursor="Hand"
                                                    ToolTip.Tip="Edit gradient color 1"
                                                    PointerPressed="OnGradientColor1PreviewPointerPressed"/>
                                            <Border Grid.Column="1"
                                                    Classes="gradient-preview-half right"
                                                    Cursor="Hand"
                                                    ToolTip.Tip="Edit gradient color 2"
                                                    PointerPressed="OnGradientColor2PreviewPointerPressed"/>
                                        </Grid>
                                    </Border>

                                    <Popup x:Name="BackgroundGradientColor1Popup"
                                           PlacementTarget="{Binding #BackgroundGradientPreviewBorder}"
                                           Placement="Bottom"
                                           IsLightDismissEnabled="True"
                                           VerticalOffset="4">
                                        <Border Classes="popup-border popup-surface"
                                                Classes.open="{Binding #BackgroundGradientColor1Popup.IsOpen}"
                                                Background="{DynamicResource ShareX.Brush.Background.Panel}"
                                                CornerRadius="8"
                                                Padding="4"
                                                BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                                BorderThickness="1">
                                            <controls:ColorPickerPanel SelectedColorValue="{Binding BackgroundGradientColor1Value, Mode=TwoWay}"
                                                                       HostPopup="{Binding #BackgroundGradientColor1Popup}"/>
                                        </Border>
                                    </Popup>

                                    <Popup x:Name="BackgroundGradientColor2Popup"
                                           PlacementTarget="{Binding #BackgroundGradientPreviewBorder}"
                                           Placement="Bottom"
                                           IsLightDismissEnabled="True"
                                           VerticalOffset="4">
                                        <Border Classes="popup-border popup-surface"
                                                Classes.open="{Binding #BackgroundGradientColor2Popup.IsOpen}"
                                                Background="{DynamicResource ShareX.Brush.Background.Panel}"
                                                CornerRadius="8"
                                                Padding="4"
                                                BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                                BorderThickness="1">
                                            <controls:ColorPickerPanel SelectedColorValue="{Binding BackgroundGradientColor2Value, Mode=TwoWay}"
                                                                       HostPopup="{Binding #BackgroundGradientColor2Popup}"/>
                                        </Border>
                                    </Popup>
                                </Grid>

                                <ItemsControl ItemsSource="{Binding GradientPresets}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="4"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:MainViewModel+GradientPreset">
                                            <Button Classes="transparent-button"
                                                    Padding="0"
                                                    Margin="2"
                                                    ToolTip.Tip="{Binding Name}"
                                                    Command="{ReflectionBinding DataContext.ApplyGradientPresetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}">
                                                <Border Width="46"
                                                        Height="36"
                                                        CornerRadius="5"
                                                        BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                                        BorderThickness="1"
                                                        Background="{Binding Brush}"/>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel IsVisible="{Binding IsColorBackgroundModeSelected}" Spacing="6">
                                <Grid Margin="0,0,0,4">
                                    <Border x:Name="BackgroundColorPreviewBorder"
                                            Height="40"
                                            CornerRadius="5"
                                            BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                            BorderThickness="1"
                                            ClipToBounds="True">
                                        <Grid>
                                            <Border CornerRadius="5"
                                                    Background="{DynamicResource ShareX.Brush.Checkered.Small}"/>
                                            <Border CornerRadius="5"
                                                    Background="{Binding BackgroundColorBrush}"/>
                                            <Border Classes="color-preview-overlay"
                                                    Cursor="Hand"
                                                    ToolTip.Tip="Edit background color"
                                                    PointerPressed="OnBackgroundColorPreviewPointerPressed"/>
                                        </Grid>
                                    </Border>

                                    <Popup x:Name="BackgroundColorPopup"
                                           PlacementTarget="{Binding #BackgroundColorPreviewBorder}"
                                           Placement="Bottom"
                                           IsLightDismissEnabled="True"
                                           VerticalOffset="4">
                                        <Border Classes="popup-border popup-surface"
                                                Classes.open="{Binding #BackgroundColorPopup.IsOpen}"
                                                Background="{DynamicResource ShareX.Brush.Background.Panel}"
                                                CornerRadius="8"
                                                Padding="4"
                                                BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                                BorderThickness="1">
                                            <controls:ColorPickerPanel SelectedColorValue="{Binding BackgroundColorValue, Mode=TwoWay}"
                                                                       HostPopup="{Binding #BackgroundColorPopup}"/>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </StackPanel>

                            <StackPanel IsVisible="{Binding IsImageBackgroundModeSelected}" Spacing="6">
                                <Button x:Name="BrowseBackgroundImageButton"
                                        Content="Browse background image..."
                                        Click="OnBrowseBackgroundImageClick"
                                        HorizontalAlignment="Stretch"/>

                                <StackPanel IsVisible="{Binding HasBackgroundImagePath}" Spacing="2">
                                    <TextBlock Text="Background image"
                                               FontWeight="Normal"
                                               FontSize="11"
                                               Foreground="{DynamicResource ShareX.Brush.Text.Secondary}"/>
                                    <TextBlock Text="{Binding BackgroundImagePath}"
                                               FontWeight="Normal"
                                               FontSize="11"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Central Preview Canvas -->
            <Grid Grid.Column="1" Margin="8">
                <ScrollViewer Name="CanvasScrollViewer"
                              Background="Transparent"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto"
                              ScrollChanged="OnCanvasScrollChanged"
                              PointerPressed="OnScrollViewerPointerPressed"
                              PointerMoved="OnScrollViewerPointerMoved"
                              PointerReleased="OnScrollViewerPointerReleased">
                    <LayoutTransformControl HorizontalAlignment="Center" VerticalAlignment="Center">
                        <LayoutTransformControl.LayoutTransform>
                            <ScaleTransform ScaleX="{Binding EffectiveZoom}" ScaleY="{Binding EffectiveZoom}" />
                        </LayoutTransformControl.LayoutTransform>
                        <!-- Main Canvas Container with Checkered Background and Shadow -->
                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                            <!-- Checkered Background -->
                            <Border BorderThickness="1"
                                    BorderBrush="{DynamicResource ShareX.Brush.Border}"
                                    Background="{DynamicResource ShareX.Brush.Checkered}"/>

                            <!-- Actual Content -->
                            <Border x:Name="PreviewFrame"
                                    IsVisible="{Binding HasPreviewImage}"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Stretch"
                                     Background="{Binding EffectiveCanvasBackground}"
                                     Padding="{Binding CanvasPadding}"
                                     PointerPressed="OnCanvasPointerPressed">
                                <!-- Outer Border: User-controlled Shadow effect -->
                                <Border BoxShadow="{Binding CanvasShadow}"
                                       CornerRadius="{Binding CanvasCornerRadius, Converter={StaticResource DoubleToCornerRadius}}">
                                    <!-- Padding Border: Uses top-left pixel color -->
                                    <Border Background="{Binding SmartPaddingColor}"
                                            Padding="{Binding SmartPaddingThickness}"
                                            CornerRadius="{Binding CanvasCornerRadius, Converter={StaticResource DoubleToCornerRadius}}"
                                            ClipToBounds="True">
                                        <Canvas Background="Transparent"
                                                Width="{Binding SmartPaddingViewportWidth}"
                                                Height="{Binding SmartPaddingViewportHeight}"
                                                ClipToBounds="False">
                                            <!-- Inner Border: Smart padding crops by clipping the viewport,
                                                 while the source image/canvas stay at full size. -->
                                            <Border Background="Transparent"
                                                    Width="{Binding SmartPaddingViewportWidth}"
                                                    Height="{Binding SmartPaddingViewportHeight}"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    ClipToBounds="True"
                                                    Canvas.Left="0"
                                                    Canvas.Top="0">
                                                <Canvas Background="Transparent"
                                                        Width="{Binding SmartPaddingViewportWidth}"
                                                        Height="{Binding SmartPaddingViewportHeight}"
                                                        ClipToBounds="True">
                                                    <Grid Name="CanvasContainer"
                                                          Background="Transparent"
                                                          Width="{Binding ImageWidth}"
                                                          Height="{Binding ImageHeight}"
                                                          Canvas.Left="{Binding SmartPaddingOffsetX}"
                                                          Canvas.Top="{Binding SmartPaddingOffsetY}">

                                                        <!-- Image layer (Hybrid Rendering: using SkiaSharp) -->
                                                        <controls:SKCanvasControl Name="CanvasControl"
                                                                                  HorizontalAlignment="Stretch"
                                                                                  VerticalAlignment="Stretch"/>

                                                        <!-- Shared spotlight overlay rendered once for all spotlight annotations -->
                                                        <controls:SpotlightOverlayControl Name="SpotlightOverlayControl"
                                                                         HorizontalAlignment="Stretch"
                                                                         VerticalAlignment="Stretch"
                                                                         IsHitTestVisible="False"
                                                                         IsVisible="False"/>

                                                        <!-- Annotation Layer (Saved content) -->
                                                        <Canvas Name="AnnotationCanvas"
                                                                Background="Transparent"
                                                                IsVisible="{Binding HasPreviewImage}"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"
                                                                Cursor="Arrow"
                                                                PointerMoved="OnCanvasPointerMoved"
                                                                PointerReleased="OnCanvasPointerReleased">
                                                        </Canvas>
                                                    </Grid>
                                                </Canvas>
                                            </Border>
                                        </Canvas>
                                    </Border>
                                </Border>
                            </Border>

                            <!-- Isolated visual host for the optional Konami-code shader animation. -->
                            <easterEggs:ShaderEasterEggOverlay x:Name="EasterEggOverlay"
                                                                  IsVisible="False"
                                                                  IsHitTestVisible="False"/>
                        </Grid>
                    </LayoutTransformControl>
                </ScrollViewer>

                <Canvas Name="OverlayHost"
                        ClipToBounds="False"
                        Panel.ZIndex="10">
                    <Canvas Name="OverlayCanvas"
                            Width="0"
                            Height="0"
                            ClipToBounds="False"
                            IsHitTestVisible="True"
                            Cursor="Arrow"
                            PointerPressed="OnCanvasPointerPressed"
                            PointerMoved="OnCanvasPointerMoved"
                            PointerReleased="OnCanvasPointerReleased">
                        <Rectangle Name="CropOverlay"
                                   IsVisible="False"
                                   Stroke="White"
                                   StrokeThickness="2"
                                   StrokeDashArray="4,2"
                                   Fill="#80000000" />
                    </Canvas>
                </Canvas>
            </Grid>

            <!-- Right Sidebar (Effects Panel) -->
            <Border Grid.Column="2"
                    Classes="editor-sidebar right"
                    Classes.open="{Binding IsRightEffectsSidebarVisible}"
                    Padding="0"
                    Background="{DynamicResource ShareX.Brush.Background.Panel}"
                    BorderBrush="{DynamicResource ShareX.Brush.Border}"
                    IsHitTestVisible="{Binding IsRightEffectsSidebarVisible}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <StackPanel Width="250" Spacing="0" Margin="16">
                            <ContentControl Content="{Binding EffectsPanelContent}" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

        </Grid>

        <!-- Row 0: Shared Annotation Toolbar - OVERLAY -->
        <Border Grid.Row="0"
            Classes="editor-toolbar-host top"
            Classes.hidden="{Binding AreToolbarsHidden}"
            Margin="0,0,0,0"
            VerticalAlignment="Top"
            HorizontalAlignment="Center"
            IsHitTestVisible="{Binding ShowToolbars}">
            <controls:AnnotationToolbar x:Name="AnnotationToolbarControl"
                                        DataContext="{Binding ToolbarAdapter}"
                                        ShowEditingActions="False"
                                        MaxWidth="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Border>

        <!-- Row 0: Export Actions Toolbar (Bottom Centered, Floating) -->
        <Border Grid.Row="0"
            Classes="editor-toolbar-host bottom"
            Classes.hidden="{Binding AreToolbarsHidden}"
            VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,0"
                Padding="4" Background="{DynamicResource ShareX.Brush.Background.Toolbar}" BorderBrush="{DynamicResource ShareX.Brush.Border}" BorderThickness="1" CornerRadius="4,4,0,0"
                MaxWidth="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
            ClipToBounds="False"
            IsHitTestVisible="{Binding ShowToolbars}">
            <StackPanel Orientation="Horizontal" Spacing="4" ClipToBounds="False">
                <!-- Zoom Picker -->
                <controls:ZoomPickerDropdown
                    SelectedZoom="{Binding Zoom}"
                    ZoomChanged="OnZoomChanged"
                    ZoomToFitRequested="OnZoomPickerZoomToFitRequested"
                    Height="40"
                    ToolTip.Tip="Zoom (Ctrl + Wheel)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5"/>

                <!-- Separator -->
                <Border Width="1" Height="38" Background="{DynamicResource ShareX.Brush.Separator}" Margin="4,0"
                        IsVisible="{Binding ShowBottomToolbar}"/>

                <!-- Task Mode Buttons -->
                <Grid Width="90" Height="40" Margin="4,0,10,0" ClipToBounds="False" VerticalAlignment="Bottom"
                      IsVisible="{Binding ShowTaskButtons}">
                    <!-- Cancel Button: Red Rectangle (Behind) -->
                    <Button Command="{Binding CancelCommand}"
                            Classes="task-mode-button danger"
                            BorderThickness="1"
                            Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                            Width="55" Margin="0,0,0,0" Padding="27,0,10,0" CornerRadius="0,4,4,0"
                            ToolTip.Tip="Cancel (Esc)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5"
                            ZIndex="0">
                        <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionCancel}"
                                   FontFamily="{DynamicResource ShareX.FontFamily.Icon}"
                                   FontSize="18" FontWeight="Normal"
                                   VerticalAlignment="Center"/>
                    </Button>

                    <!-- Continue Button: Big Green Circle (Front) -->
                    <Button Command="{Binding ContinueCommand}"
                            Classes="task-mode-button success"
                            Width="56" Height="56" CornerRadius="28"
                            BorderThickness="1"
                            HorizontalAlignment="Left" VerticalAlignment="Bottom"
                            Margin="0,0,0,-8"
                            ToolTip.Tip="{Binding ContinueButtonTooltip}" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5"
                            ZIndex="20">
                        <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionContinue}"
                                   FontFamily="{DynamicResource ShareX.FontFamily.Icon}"
                                   FontSize="22" FontWeight="Normal"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Margin="4,0,0,0"/>
                    </Button>
                </Grid>

                <Button Command="{Binding CopyCommand}" Width="40" Padding="0"
                        IsVisible="{Binding ShowBottomToolbar}"
                        ToolTip.Tip="Copy image to clipboard (Ctrl+C)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5">
                    <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionCopy}" FontSize="18" FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal"/>
                </Button>

                <Button Command="{Binding SaveCommand}" Width="40" Padding="0"
                        IsVisible="{Binding ShowBottomToolbar}"
                        ToolTip.Tip="Save image (Ctrl+S)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5">
                    <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionSave}" FontSize="18" FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal"/>
                </Button>

                <Button Command="{Binding SaveAsCommand}" Width="40" Padding="0"
                        IsVisible="{Binding ShowBottomToolbar}"
                        ToolTip.Tip="Save image as... (Ctrl+Shift+S)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5">
                    <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionSaveAs}" FontSize="18" FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal"/>
                </Button>

                <Button Command="{Binding PinToScreenCommand}" IsEnabled="{Binding HasPreviewImage}" Width="40" Padding="0"
                        IsVisible="{Binding ShowBottomToolbar}"
                        ToolTip.Tip="Pin image to screen (Ctrl+P)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5">
                    <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionPinToScreen}" FontSize="18" FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal"/>
                </Button>

                <Button Command="{Binding PrintCommand}" IsEnabled="{Binding HasPreviewImage}" Width="40" Padding="0"
                        IsVisible="{Binding ShowBottomToolbar}"
                        ToolTip.Tip="Print image... (Ctrl+Shift+P)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5">
                    <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionPrint}" FontSize="18" FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal"/>
                </Button>

                <Button Command="{Binding UploadCommand}" IsEnabled="{Binding HasPreviewImage}" Width="40" Padding="0"
                        IsVisible="{Binding ShowBottomToolbar}"
                        ToolTip.Tip="Upload image (Ctrl+U)" ToolTip.Placement="Top" ToolTip.VerticalOffset="-5">
                    <TextBlock Text="{x:Static editorTheming:EditorIcons.ActionUpload}" FontSize="18" FontFamily="{DynamicResource ShareX.FontFamily.Icon}" FontWeight="Normal"/>
                </Button>
            </StackPanel>
        </Border>

        <Border Classes="popup-surface zoom-upward app-toast"
                Classes.open="{Binding IsNotificationOpen}"
                Grid.Row="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="16,0,16,68"
                MaxWidth="{Binding Bounds.Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsVisible="{Binding IsNotificationVisible}"
                Cursor="Hand"
                PointerEntered="OnNotificationPointerEntered"
                PointerExited="OnNotificationPointerExited"
                PointerPressed="OnNotificationPointerPressed"
                ZIndex="28">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                <TextBlock Grid.Column="0"
                           Text="{Binding NotificationIcon}"
                           FontFamily="{DynamicResource ShareX.FontFamily.Icon}"
                           FontWeight="Normal"
                           FontSize="18"
                           VerticalAlignment="Center"
                           IsVisible="{Binding HasNotificationIcon}"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding NotificationMessage}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource ShareX.Brush.Text}"/>
            </Grid>
        </Border>

        <Border x:Name="InteractionCaptureLayer"
                Grid.Row="0"
                Background="Transparent"
                IsVisible="False"
                IsHitTestVisible="False"
                Cursor="Arrow"
                ZIndex="25"
                PointerPressed="OnCanvasPointerPressed"
                PointerMoved="OnCanvasPointerMoved"
                PointerReleased="OnCanvasPointerReleased"/>

        <!-- Effect Browser Panel Overlay -->
        <Grid Grid.Row="0"
              IsHitTestVisible="{Binding IsEffectBrowserVisible}"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              ZIndex="30">

            <!-- Click-away background to close the panel -->
            <Grid Classes="overlay-scrim"
                  Classes.open="{Binding IsEffectBrowserVisible}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  PointerPressed="OnEffectBrowserOverlayPointerPressed" />

            <!-- The Panel Itself -->
            <Border Classes="floating-panel"
                    Classes.open="{Binding IsEffectBrowserVisible}"
                    Background="{DynamicResource ShareX.Brush.Background.Panel}"
                    BorderBrush="{DynamicResource ShareX.Brush.Border}"
                    BorderThickness="1"
                    CornerRadius="8"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="16,125,16,125"
                    Width="800">

                <ContentControl x:Name="EffectBrowserHost"/>
            </Border>
        </Grid>

        <!-- Modal Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="1"
                Classes="modal-scrim"
                IsVisible="{Binding IsModalOpen}"
                IsHitTestVisible="True">
            <Grid>
                <!-- Click outside to close -->
                <Border Background="Transparent"
                        PointerPressed="OnModalBackgroundPressed"/>

                <!-- Modal Content -->
                <ContentControl Content="{Binding ModalContent}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
            </Grid>
        </Border>
        </Grid>
    </ThemeVariantScope>
</UserControl>
